# Kafka / Redpanda
KAFKA_BROKER=localhost:19092
MATCH_TOPIC=match.events

# Which StatsBomb open-data match to replay (default: 2022 World Cup Final)
MATCH_ID=3869685

# Replay speed multiplier (60 = a 90-min match streams in ~90 seconds)
REPLAY_SPEED=60
