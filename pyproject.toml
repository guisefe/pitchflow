[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",
    "F401",
]

[tool.pytest.ini_options]
testpaths = ["producer/tests", "streaming/tests"]
addopts = "-v --tb=short"
